import "CustomJoke"
class JokeFactory {
    public static createCustomJokes(jsonArray: string) : CustomJoke[]
    {
        JSON.parse(jsonArray);  //: object
        JSON.stringify(obj): ""

        //TOTO = '[{"setup":"..." ; ...}]'
    }
}